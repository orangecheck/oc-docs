export const metadata = {
    title: "Function: isSudoAccountMismatch()",
    description: "Auto-generated API reference for Function: isSudoAccountMismatch(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/auth-client**](../README)

***

[@orangecheck/auth-client](../README) / isSudoAccountMismatch

# Function: isSudoAccountMismatch()

```ts
function isSudoAccountMismatch(body: 
  | {
  reason?: string;
}
  | null
  | undefined): boolean;
```

Defined in: [sudo.tsx:101](https://github.com/orangecheck/oc-packages/blob/main/auth-client/src/sudo.tsx#L101)

True when the response says a sudo ceremony cannot resolve this request.
Pairs with [handleSudoRequired](handleSudoRequired): try the redirect first, then this.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `body` | \| \&#123; `reason?`: `string`; \&#125; \| `null` \| `undefined` |

## Returns

`boolean`
