export const metadata = {
    title: "Function: Toaster()",
    description: "Auto-generated API reference for Function: Toaster(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / Toaster

# Function: Toaster()

```ts
function Toaster(__namedParameters: ToasterProps): any;
```

Defined in: [primitives/toaster.tsx:10](https://github.com/orangecheck/oc-packages/blob/main/design/src/primitives/toaster.tsx#L10)

Toast host in the house design language. Mount once at the app root, then
call `toast()` from `sonner` anywhere. Re-exported here for convenience.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | `ToasterProps` |

## Returns

`any`
