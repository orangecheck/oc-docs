export const metadata = {
    title: "Function: makeDefaultAnchorVerifier()",
    description: "Auto-generated API reference for Function: makeDefaultAnchorVerifier(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/stamp-ots**](../README)

***

[@orangecheck/stamp-ots](../README) / makeDefaultAnchorVerifier

# Function: makeDefaultAnchorVerifier()

```ts
function makeDefaultAnchorVerifier(opts: {
  headerSource: BlockHeaderSource;
}): Promise<(proofB64: string, blockHeight: number, blockHash: string, envelopeId: string) => Promise<boolean>>;
```

Defined in: [anchor.ts:113](https://github.com/orangecheck/oc-packages/blob/main/stamp-ots/src/anchor.ts#L113)

Anchor verifier using the built-in OTS parser. Kept async for callers of the
0.1 API, which lazily loaded an optional parser here.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `opts` | \&#123; `headerSource`: [`BlockHeaderSource`](../interfaces/BlockHeaderSource); \&#125; |
| `opts.headerSource` | [`BlockHeaderSource`](../interfaces/BlockHeaderSource) |

## Returns

`Promise`\&lt;(`proofB64`: `string`, `blockHeight`: `number`, `blockHash`: `string`, `envelopeId`: `string`) => `Promise`\&lt;`boolean`\>\>
