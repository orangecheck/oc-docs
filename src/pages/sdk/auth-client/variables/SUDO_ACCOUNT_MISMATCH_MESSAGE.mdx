export const metadata = {
    title: "Variable: SUDOACCOUNTMISMATCHMESSAGE",
    description: "Auto-generated API reference for Variable: SUDOACCOUNTMISMATCHMESSAGE. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/auth-client**](../README)

***

[@orangecheck/auth-client](../README) / SUDO\_ACCOUNT\_MISMATCH\_MESSAGE

# Variable: SUDO\_ACCOUNT\_MISMATCH\_MESSAGE

```ts
const SUDO_ACCOUNT_MISMATCH_MESSAGE: "Switch this tab to that account first — the confirmation on file is for a different one." = 'Switch this tab to that account first — the confirmation on file is for a different one.';
```

Defined in: [sudo.tsx:94](https://github.com/orangecheck/oc-packages/blob/main/auth-client/src/sudo.tsx#L94)

The message for `sudo_account_mismatch` — a fresh sudo proof exists, but for
a different account than this tab is acting as.

Deliberately NOT a redirect. The ceremony runs on ochk.io for the cookie's
default account and cannot reach this tab's pinned account, so bouncing there
re-proves the wrong one forever. Callers must show this and stop.
