export const metadata = {
    title: "Function: canonicalizeToolUse()",
    description: "Auto-generated API reference for Function: canonicalizeToolUse(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/agent-anthropic**](../README.mdx)

***

[@orangecheck/agent-anthropic](../README.mdx) / canonicalizeToolUse

# Function: canonicalizeToolUse()

```ts
function canonicalizeToolUse(t: AnthropicToolUse): Uint8Array;
```

Defined in: [index.ts:49](https://github.com/orangecheck/oc-packages/blob/main/agent-anthropic/src/index.ts#L49)

Produce the canonical bytes for an Anthropic tool_use. The bytes are what
the agent-action envelope's `content.hash` is a SHA-256 of.

Shape (RFC 8785 canonical JSON, then trailing LF):
  &#123;
    "id":    &lt;tool_use_id>,
    "input": &lt;canonical JSON of input>,
    "name":  &lt;tool name>
  &#125;

## Parameters

| Parameter | Type |
| ------ | ------ |
| `t` | [`AnthropicToolUse`](../interfaces/AnthropicToolUse.mdx) |

## Returns

`Uint8Array`
