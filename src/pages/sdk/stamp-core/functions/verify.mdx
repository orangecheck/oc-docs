export const metadata = {
    title: "Function: verify()",
    description: "Auto-generated API reference for Function: verify(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/stamp-core**](../README.mdx)

***

[@orangecheck/stamp-core](../README.mdx) / verify

# Function: verify()

```ts
function verify(input: VerifyInput): Promise<VerifyResult>;
```

Defined in: [stamp-core/src/stamp.ts:133](https://github.com/orangecheck/oc-packages/blob/main/stamp-core/src/stamp.ts#L133)

Verify a stamp envelope. See SPEC §8 for the full algorithm. Returns a
structured result; callers can match on `result.ok` and drill into
`result.code` for the specific failure.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`VerifyInput`](../interfaces/VerifyInput.mdx) |

## Returns

`Promise`\&lt;[`VerifyResult`](../type-aliases/VerifyResult.mdx)\>
