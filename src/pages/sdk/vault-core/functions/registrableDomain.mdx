export const metadata = {
    title: "Function: registrableDomain()",
    description: "Auto-generated API reference for Function: registrableDomain(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vault-core**](../README.mdx)

***

[@orangecheck/vault-core](../README.mdx) / registrableDomain

# Function: registrableDomain()

```ts
function registrableDomain(host: string): string;
```

Defined in: [vault-core/src/origin.ts:46](https://github.com/orangecheck/oc-packages/blob/main/vault-core/src/origin.ts#L46)

The registrable domain (eTLD+1) of a hostname — `mail.example.co.uk`
→ `example.co.uk`. Falls back to the full host when the suffix is
unknown (stricter, never looser).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `host` | `string` |

## Returns

`string`
