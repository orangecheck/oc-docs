export const metadata = {
    title: "Function: isMainnetAddress()",
    description: "Auto-generated API reference for Function: isMainnetAddress(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vote-core**](../README)

***

[@orangecheck/vote-core](../README) / isMainnetAddress

# Function: isMainnetAddress()

```ts
function isMainnetAddress(addr: unknown): boolean;
```

Defined in: [vote-core/src/verify.ts:34](https://github.com/orangecheck/oc-packages/blob/main/vote-core/src/verify.ts#L34)

SPEC §3.3 / §4.3: creators and voters MUST use Bitcoin mainnet addresses.
This is a network check by prefix only (bech32 `bc1`, base58 `1` / `3`);
checksum validity is left to the BIP-322 verifier.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `addr` | `unknown` |

## Returns

`boolean`
