export const metadata = {
    title: "Function: configure()",
    description: "Auto-generated API reference for Function: configure(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/insights-client**](../README)

***

[@orangecheck/insights-client](../README) / configure

# Function: configure()

```ts
function configure(config: InsightsClientConfig): void;
```

Defined in: [index.ts:215](https://github.com/orangecheck/oc-packages/blob/main/insights-client/src/index.ts#L215)

Override the default client's config (e.g. inject a token or fetch).
Resets the lazily-built default so the next emit picks up the change.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `config` | [`InsightsClientConfig`](../interfaces/InsightsClientConfig) |

## Returns

`void`
