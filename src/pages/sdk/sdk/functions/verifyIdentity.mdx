export const metadata = {
    title: "Function: verifyIdentity()",
    description: "Auto-generated API reference for Function: verifyIdentity(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / verifyIdentity

# Function: verifyIdentity()

```ts
function verifyIdentity(
   attestationId: string, 
   identity: IdentityBinding, 
options?: VerifyIdentityOptions): Promise<VerificationResult>;
```

Defined in: [identity/index.ts:65](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/identity/index.ts#L65)

Verify an identity binding

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `attestationId` | `string` | The attestation ID to verify against |
| `identity` | [`IdentityBinding`](../interfaces/IdentityBinding.mdx) | The identity binding to verify |
| `options?` | [`VerifyIdentityOptions`](../interfaces/VerifyIdentityOptions.mdx) | Protocol-specific verification options |

## Returns

`Promise`\&lt;[`VerificationResult`](../type-aliases/VerificationResult.mdx)\>

Verification result with verified status and optional error

## Example

```typescript
const result = await verifyIdentity(
  'abc123...',
  { protocol: 'nostr', identifier: 'npub1...' },
  { relays: ['wss://relay.damus.io'] }
);

if (result.verified) {
  console.log('Identity verified!');
}
```
