export const metadata = {
    title: "Function: verify()",
    description: "Auto-generated API reference for Function: verify(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/webhook-verify**](../README.mdx)

***

[@orangecheck/webhook-verify](../README.mdx) / verify

# Function: verify()

```ts
function verify(input: VerifyInput): boolean;
```

Defined in: [index.ts:61](https://github.com/orangecheck/oc-packages/blob/main/webhook-verify/src/index.ts#L61)

Returns true iff the signature is a valid HMAC-SHA256 of rawBody under
secret. Uses timing-safe compare so a malicious server can't byte-by-
byte probe the expected sig.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`VerifyInput`](../interfaces/VerifyInput.mdx) |

## Returns

`boolean`
