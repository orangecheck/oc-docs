export const metadata = {
    title: "Function: OcFamilyFooter()",
    description: "Auto-generated API reference for Function: OcFamilyFooter(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / OcFamilyFooter

# Function: OcFamilyFooter()

```ts
function OcFamilyFooter(__namedParameters: OcFamilyFooterProps): any;
```

Defined in: [chrome/family-footer.tsx:122](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/family-footer.tsx#L122)

`<OcFamilyFooter>` — the one canonical family footer. Brand block on the
left, `§`-labelled link columns to the right, a single legal bar beneath.
Every `oc-*` site composes this with its own `brand` + `columns`; pass
`family` to surface the registry-driven product / protocol columns so any
footer listing the family always shows the full, current set.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`OcFamilyFooterProps`](../interfaces/OcFamilyFooterProps) |

## Returns

`any`
