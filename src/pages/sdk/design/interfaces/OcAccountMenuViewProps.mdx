export const metadata = {
    title: "Interface: OcAccountMenuViewProps",
    description: "Auto-generated API reference for Interface: OcAccountMenuViewProps. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / OcAccountMenuViewProps

# Interface: OcAccountMenuViewProps

Defined in: [chrome/account-menu.tsx:616](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L616)

## Extends

- [`OcAccountMenuProps`](OcAccountMenuProps)

## Properties

| Property | Type | Description | Inherited from | Defined in |
| ------ | ------ | ------ | ------ | ------ |
| &lt;a id="property-build">&lt;/a> `build?` | [`OcAccountMenuBuildInfo`](OcAccountMenuBuildInfo) | Optional BuildFooter info. When present, renders a tiny mono footer at the bottom of the popover with `site · vX.Y.Z · sha` — the SHA links to the matching GitHub commit when `repo` is also provided. Drive this from your `next.config.ts` via `NEXT_PUBLIC_APP_VERSION` / `NEXT_PUBLIC_BUILD_SHA` env vars. | [`OcAccountMenuProps`](OcAccountMenuProps).[`build`](OcAccountMenuProps#property-build) | [chrome/account-menu.tsx:215](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L215) |
| &lt;a id="property-classname">&lt;/a> `className?` | `string` | className overrides for layout-level customisation. | [`OcAccountMenuProps`](OcAccountMenuProps).[`className`](OcAccountMenuProps#property-classname) | [chrome/account-menu.tsx:223](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L223) |
| &lt;a id="property-current">&lt;/a> `current` | [`EcosystemSlug`](../type-aliases/EcosystemSlug) | Which family property this site IS. Used to label the section header (`§ signed in · vault.ochk.io`) and to suppress the family-dashboard menu item when the user is already on `home`. | [`OcAccountMenuProps`](OcAccountMenuProps).[`current`](OcAccountMenuProps#property-current) | [chrome/account-menu.tsx:155](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L155) |
| &lt;a id="property-menuitems">&lt;/a> `menuItems?` | readonly [`OcAccountMenuItem`](OcAccountMenuItem)[] | Site-specific menu items, rendered between the section header and the family-dashboard / sign-out rows. Keep this short — 2–6 entries is the sweet spot. Pass `[]` (or omit) for sites with no per-site routes worth surfacing. | [`OcAccountMenuProps`](OcAccountMenuProps).[`menuItems`](OcAccountMenuProps#property-menuitems) | [chrome/account-menu.tsx:184](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L184) |
| &lt;a id="property-popoverclassname">&lt;/a> `popoverClassName?` | `string` | - | [`OcAccountMenuProps`](OcAccountMenuProps).[`popoverClassName`](OcAccountMenuProps#property-popoverclassname) | [chrome/account-menu.tsx:225](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L225) |
| &lt;a id="property-primarynavlinks">&lt;/a> `primaryNavLinks?` | readonly [`OcAccountMenuItem`](OcAccountMenuItem)[] | Centered primary nav links (typically dashboard · docs · spec). Rendered inside the popover at viewport widths below `sm` (640px) — these are the same links the `<OcPrimaryNav>` row shows inline at sm+, but at ultra-small widths the inline row hides and this popover section takes its place. The result: one dropdown surface owns navigation on phones, no separate hamburger drawer required. | [`OcAccountMenuProps`](OcAccountMenuProps).[`primaryNavLinks`](OcAccountMenuProps#property-primarynavlinks) | [chrome/account-menu.tsx:194](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L194) |
| &lt;a id="property-secondarynavlinks">&lt;/a> `secondaryNavLinks?` | readonly [`OcAccountMenuItem`](OcAccountMenuItem)[] | Tertiary nav links (about / contact / status / privacy / etc.). Always visible inside the popover. Folds the legacy mobile-only hamburger drawer's contents into the same dropdown as everything else, so there's exactly one nav surface to learn. | [`OcAccountMenuProps`](OcAccountMenuProps).[`secondaryNavLinks`](OcAccountMenuProps#property-secondarynavlinks) | [chrome/account-menu.tsx:201](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L201) |
| &lt;a id="property-session">&lt;/a> `session` | [`OcAccountMenuSession`](OcAccountMenuSession) | The session to render. When using `<OcAccountMenuView>` directly (rather than the connected `<OcAccountMenu>`), pass an adapter over your local auth context. | - | [chrome/account-menu.tsx:622](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L622) |
| &lt;a id="property-showfamilydashboard">&lt;/a> `showFamilyDashboard?` | `boolean` | When `true` (default), shows a `family dashboard ↗` link to `https://ochk.io/dashboard`. Set `false` for `home` (already there) or for sites that don't want it. | [`OcAccountMenuProps`](OcAccountMenuProps).[`showFamilyDashboard`](OcAccountMenuProps#property-showfamilydashboard) | [chrome/account-menu.tsx:207](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L207) |
| &lt;a id="property-signinlabel">&lt;/a> `signInLabel?` | `string` | Label for the anonymous "sign in" affordance. Default `'sign in'`. | [`OcAccountMenuProps`](OcAccountMenuProps).[`signInLabel`](OcAccountMenuProps#property-signinlabel) | [chrome/account-menu.tsx:164](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L164) |
| &lt;a id="property-signinurl">&lt;/a> `signInUrl?` | `string` | Where the anonymous "sign in" affordance points. Default `'/signin'` — works with every site's in-place `<OcSignIn>` page. Pass `'https://ochk.io/signin'` if you want to bounce to the auth host instead. | [`OcAccountMenuProps`](OcAccountMenuProps).[`signInUrl`](OcAccountMenuProps#property-signinurl) | [chrome/account-menu.tsx:162](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L162) |
| &lt;a id="property-signoutredirect">&lt;/a> `signOutRedirect?` | `string` | Where the browser lands after the user signs out. Default `'/'` — the current site's own home. On sign-out the menu fires `signOut()` (whose logout round-trip is `keepalive`, so it survives the navigation) and *immediately* hard-navigates here. Navigating in the same tick is deliberate: it forecloses the race where an auth-gated page observes the session flip to `anonymous` and runs its own redirect-to-sign-in first. Sign-out means "leave", not "go sign in again" — so the family default is the site's home, never a sign-in page or the auth host. | [`OcAccountMenuProps`](OcAccountMenuProps).[`signOutRedirect`](OcAccountMenuProps#property-signoutredirect) | [chrome/account-menu.tsx:177](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L177) |
| &lt;a id="property-sitestate">&lt;/a> `siteState?` | [`SiteState`](../type-aliases/SiteState) | Optional lifecycle chip rendered inside the BuildFooter line (between site and version). Mirrors the chip on the logo so both surfaces agree. | [`OcAccountMenuProps`](OcAccountMenuProps).[`siteState`](OcAccountMenuProps#property-sitestate) | [chrome/account-menu.tsx:221](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L221) |
| &lt;a id="property-triggerclassname">&lt;/a> `triggerClassName?` | `string` | - | [`OcAccountMenuProps`](OcAccountMenuProps).[`triggerClassName`](OcAccountMenuProps#property-triggerclassname) | [chrome/account-menu.tsx:224](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L224) |
