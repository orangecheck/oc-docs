export const metadata = {
    title: "Function: pendingCommitments()",
    description: "Auto-generated API reference for Function: pendingCommitments(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/stamp-ots**](../README)

***

[@orangecheck/stamp-ots](../README) / pendingCommitments

# Function: pendingCommitments()

```ts
function pendingCommitments(ts: OtsTimestamp): {
  commitment: Uint8Array;
  uri: string;
}[];
```

Defined in: [ots.ts:207](https://github.com/orangecheck/oc-packages/blob/main/stamp-ots/src/ots.ts#L207)

Pending calendar attestations: where to ask, and the commitment to ask for.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ts` | [`OtsTimestamp`](../interfaces/OtsTimestamp) |

## Returns

\&#123;
  `commitment`: `Uint8Array`;
  `uri`: `string`;
\&#125;[]
