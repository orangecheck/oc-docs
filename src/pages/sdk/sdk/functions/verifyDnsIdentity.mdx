export const metadata = {
    title: "Function: verifyDnsIdentity()",
    description: "Auto-generated API reference for Function: verifyDnsIdentity(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / verifyDnsIdentity

# Function: verifyDnsIdentity()

```ts
function verifyDnsIdentity(attestationId: string, domain: string): Promise<DnsVerificationResult>;
```

Defined in: [identity/dns.ts:56](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/identity/dns.ts#L56)

Verify that a user controls a DNS domain

Verification methods (in order of preference):
1. Check .well-known/orangecheck.txt file
2. Check DNS TXT record (requires server-side DNS lookup)

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `attestationId` | `string` | The attestation ID to look for |
| `domain` | `string` | The domain name (without protocol) |

## Returns

`Promise`\&lt;[`DnsVerificationResult`](../interfaces/DnsVerificationResult.mdx)\>

Verification result
