export const metadata = {
    title: "Function: verifyGitHubIdentity()",
    description: "Auto-generated API reference for Function: verifyGitHubIdentity(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / verifyGitHubIdentity

# Function: verifyGitHubIdentity()

```ts
function verifyGitHubIdentity(attestationId: string, username: string): Promise<GitHubVerificationResult>;
```

Defined in: [identity/github.ts:86](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/identity/github.ts#L86)

Verify that a user controls a GitHub identity

Verification method:
1. Query GitHub API for user's public gists
2. Check if any gist file contains the attestation ID
3. Return the gist URL as proof

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `attestationId` | `string` | The attestation ID to look for |
| `username` | `string` | The GitHub username |

## Returns

`Promise`\&lt;[`GitHubVerificationResult`](../interfaces/GitHubVerificationResult.mdx)\>

Verification result with gist URL if verified
