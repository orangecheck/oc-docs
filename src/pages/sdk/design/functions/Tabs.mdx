export const metadata = {
    title: "Function: Tabs()",
    description: "Auto-generated API reference for Function: Tabs(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / Tabs

# Function: Tabs()

```ts
function Tabs(__namedParameters: ComponentProps<any>): any;
```

Defined in: [primitives/tabs.tsx:15](https://github.com/orangecheck/oc-packages/blob/main/design/src/primitives/tabs.tsx#L15)

Tabs — accessible tabbed interface (Radix). Replaces the hand-rolled
`role="tablist"` + manual `useState` toggles found in vault settings, the
www dashboard, me's SnippetCard, attest's CodeTabs, and others. Keyboard
nav, roving tabindex, and ARIA wiring come from Radix; the OC look (mono,
underline-on-active) is layered on top.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | `ComponentProps`\&lt;`any`\> |

## Returns

`any`
