export const metadata = {
    title: "Function: handleSudoRequired()",
    description: "Auto-generated API reference for Function: handleSudoRequired(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/auth-client**](../README.mdx)

***

[@orangecheck/auth-client](../README.mdx) / handleSudoRequired

# Function: handleSudoRequired()

```ts
function handleSudoRequired(body: 
  | {
  reason?: string;
}
  | null
  | undefined, args?: Omit<RedirectToSudoArgs, "returnTo"> & {
  returnTo?: string;
}): boolean;
```

Defined in: [sudo.tsx:75](https://github.com/orangecheck/oc-packages/blob/main/auth-client/src/sudo.tsx#L75)

Convenience wrapper · the most common consumer pattern is "if the
fetch response's body has `reason === 'sudo_required'`, redirect."
Use this to keep that idiom one line:

  const r = await fetch(...);
  const j = await r.json();
  if (handleSudoRequired(j, &#123; purpose: 'register hardware key' &#125;)) return;

Returns `true` if a redirect happened (and the caller should
short-circuit), `false` otherwise.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `body` | \| \&#123; `reason?`: `string`; \&#125; \| `null` \| `undefined` |
| `args` | `Omit`\&lt;[`RedirectToSudoArgs`](../interfaces/RedirectToSudoArgs.mdx), `"returnTo"`\> & \&#123; `returnTo?`: `string`; \&#125; |

## Returns

`boolean`
