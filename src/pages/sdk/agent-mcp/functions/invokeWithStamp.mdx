export const metadata = {
    title: "Function: invokeWithStamp()",
    description: "Auto-generated API reference for Function: invokeWithStamp(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/agent-mcp**](../README.mdx)

***

[@orangecheck/agent-mcp](../README.mdx) / invokeWithStamp

# Function: invokeWithStamp()

```ts
function invokeWithStamp<T>(input: InvokeWithStampInput<T>): Promise<InvokeWithStampResult<T>>;
```

Defined in: [index.ts:154](https://github.com/orangecheck/oc-packages/blob/main/agent-mcp/src/index.ts#L154)

Stamp the invocation *first*, then execute the call. The stamped envelope
is returned alongside the result so the caller can ship them together.

Rationale for stamping first: if the downstream call hangs or fails, we
still have an on-record commitment that this agent attempted this specific
invocation. The server-side verifier sees a stamped intent plus the server's
own transcript.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`InvokeWithStampInput`](../interfaces/InvokeWithStampInput.mdx)\&lt;`T`\> |

## Returns

`Promise`\&lt;[`InvokeWithStampResult`](../interfaces/InvokeWithStampResult.mdx)\&lt;`T`\>\>
