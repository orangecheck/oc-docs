export const metadata = {
    title: "Function: unwrapVaultKey()",
    description: "Auto-generated API reference for Function: unwrapVaultKey(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vault-core**](../README)

***

[@orangecheck/vault-core](../README) / unwrapVaultKey

# Function: unwrapVaultKey()

```ts
function unwrapVaultKey(w: WrappedKey, passphrase: string): Uint8Array;
```

Defined in: [vault-core/src/crypto.ts:153](https://github.com/orangecheck/oc-packages/blob/main/vault-core/src/crypto.ts#L153)

Unwrap the vault key from its escrowed `WrappedKey` using the passphrase.
scrypt-derives the wrap key, then AES-256-GCM-decrypts. Throws
`WrongPassphrase` on any failure — never returns a bogus key.

Work factors are checked BEFORE any memory is spent on them, and that
failure is deliberately distinct from `WrongPassphrase`: a blob asking for
17 GiB is not a typo the user can fix.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `w` | [`WrappedKey`](../interfaces/WrappedKey) |
| `passphrase` | `string` |

## Returns

`Uint8Array`
