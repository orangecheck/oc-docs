export const metadata = {
    title: "Function: assertSignedBy()",
    description: "Auto-generated API reference for Function: assertSignedBy(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/wallet-adapter**](../README)

***

[@orangecheck/wallet-adapter](../README) / assertSignedBy

# Function: assertSignedBy()

```ts
function assertSignedBy(args: {
  address: string;
  message: string;
  signature: string;
}): Promise<void>;
```

Defined in: [wallet-adapter/src/sign.ts:60](https://github.com/orangecheck/oc-packages/blob/main/wallet-adapter/src/sign.ts#L60)

Throw `WrongAccountError` unless `signature` is a valid BIP-322 (or legacy)
signature of `message` by `address`. Every signer runs this before returning.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `args` | \&#123; `address`: `string`; `message`: `string`; `signature`: `string`; \&#125; |
| `args.address` | `string` |
| `args.message` | `string` |
| `args.signature` | `string` |

## Returns

`Promise`\&lt;`void`\>
