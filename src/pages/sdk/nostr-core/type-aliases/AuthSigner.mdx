export const metadata = {
    title: "Type Alias: AuthSigner",
    description: "Auto-generated API reference for Type Alias: AuthSigner. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/nostr-core**](../README.mdx)

***

[@orangecheck/nostr-core](../README.mdx) / AuthSigner

# Type Alias: AuthSigner

```ts
type AuthSigner = (challenge: string, relayUrl: string) => 
  | NostrEvent
| Promise<NostrEvent>;
```

Defined in: [index.ts:167](https://github.com/orangecheck/oc-packages/blob/main/nostr-core/src/index.ts#L167)

Sign a [NIP-42](https://nips.nostr.com/42) AUTH challenge. Given the relay's
`challenge` string and `relayUrl`, return a signed kind-22242 event (tags
`["relay", relayUrl]` + `["challenge", challenge]`). Passed in by the caller so
this package stays dependency-free (no crypto here). Provide it only for
`auth-required` relays; absent it, an AUTH challenge is ignored (today's behavior).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `challenge` | `string` |
| `relayUrl` | `string` |

## Returns

  \| [`NostrEvent`](../interfaces/NostrEvent.mdx)
  \| `Promise`\&lt;[`NostrEvent`](../interfaces/NostrEvent.mdx)\>
