export const metadata = {
    title: "Function: bitcoinAnchors()",
    description: "Auto-generated API reference for Function: bitcoinAnchors(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/stamp-ots**](../README)

***

[@orangecheck/stamp-ots](../README) / bitcoinAnchors

# Function: bitcoinAnchors()

```ts
function bitcoinAnchors(ts: OtsTimestamp): {
  blockHeight: number;
  merkleRoot: Uint8Array;
}[];
```

Defined in: [ots.ts:198](https://github.com/orangecheck/oc-packages/blob/main/stamp-ots/src/ots.ts#L198)

Bitcoin attestations, lowest height first. `merkleRoot` is the message at
the attestation, which equals the block header's Merkle root in header
byte order.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `ts` | [`OtsTimestamp`](../interfaces/OtsTimestamp) |

## Returns

\&#123;
  `blockHeight`: `number`;
  `merkleRoot`: `Uint8Array`;
\&#125;[]
