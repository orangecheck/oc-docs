export const metadata = {
    title: "Function: getSigner()",
    description: "Auto-generated API reference for Function: getSigner(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/wallet-adapter**](../README)

***

[@orangecheck/wallet-adapter](../README) / getSigner

# Function: getSigner()

```ts
function getSigner(wallet: WalletId, opts: SignOptions): SignFn;
```

Defined in: [sign.ts:32](https://github.com/orangecheck/oc-packages/blob/main/wallet-adapter/src/sign.ts#L32)

Return a SignFn bound to a particular wallet. The returned function takes a
canonical message string and resolves to a BIP-322 signature.

  const sign = getSigner('unisat', &#123; address &#125;);
  const signature = await sign(challenge.message);

Throws on unknown wallet IDs or when the wallet isn't available on window.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `wallet` | [`WalletId`](../type-aliases/WalletId) |
| `opts` | [`SignOptions`](../interfaces/SignOptions) |

## Returns

[`SignFn`](../type-aliases/SignFn)
