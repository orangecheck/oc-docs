export const metadata = {
    title: "Function: verifyReveal()",
    description: "Auto-generated API reference for Function: verifyReveal(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vote-core**](../README)

***

[@orangecheck/vote-core](../README) / verifyReveal

# Function: verifyReveal()

```ts
function verifyReveal(
   reveal: Reveal, 
   poll: Poll, 
verify: SignatureVerifier): Promise<VerifyResult>;
```

Defined in: [vote-core/src/verify.ts:133](https://github.com/orangecheck/oc-packages/blob/main/vote-core/src/verify.ts#L133)

SPEC §6.4 step 1 and §10.3: a reveal is accepted only if it names this poll,
is dated at or after the deadline, and is signed by the poll's creator over
`reveal_id`.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `reveal` | [`Reveal`](../interfaces/Reveal) |
| `poll` | [`Poll`](../interfaces/Poll) |
| `verify` | [`SignatureVerifier`](../type-aliases/SignatureVerifier) |

## Returns

`Promise`\&lt;[`VerifyResult`](../type-aliases/VerifyResult)\>
