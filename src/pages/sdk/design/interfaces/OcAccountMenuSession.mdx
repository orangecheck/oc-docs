export const metadata = {
    title: "Interface: OcAccountMenuSession",
    description: "Auto-generated API reference for Interface: OcAccountMenuSession. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / OcAccountMenuSession

# Interface: OcAccountMenuSession

Defined in: [chrome/account-menu.tsx:85](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L85)

Minimal session shape `<OcAccountMenuView>` needs. Compatible with
`useOcSession()`'s return value but explicit so consumers running
their own auth context (e.g. oc-www's local `useAuth`) can pass an
adapter without depending on `@orangecheck/auth-client`'s exact
types.

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="property-account">&lt;/a> `account` | \| \&#123; `didOc`: `string`; `displayIdentity`: `DisplayIdentity`; `displayName`: `string` \| `null`; `nostrNpub?`: `string` \| `null`; \&#125; \| `null` | - | [chrome/account-menu.tsx:87](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L87) |
| &lt;a id="property-addaccounturl">&lt;/a> `addAccountUrl?` | (`returnTo?`: `string`) => `string` | Multi-account · URL for the "add another account" entry point. Defaults to `/signin?add=1` when omitted, which works for every `.ochk.io` family site (in-place sign-in supports the query param by default). | [chrome/account-menu.tsx:137](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L137) |
| &lt;a id="property-refresh">&lt;/a> `refresh` | () => `void` \| `Promise`\&lt;`void`\> | - | [chrome/account-menu.tsx:108](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L108) |
| &lt;a id="property-roster">&lt;/a> `roster?` | `OcAccountSummary`[] | Multi-account roster · other accounts in this browser the user can switch to without re-authenticating. Optional: when omitted (or empty), the `§ accounts` section in `<OcAccountMenu>` is suppressed and the menu renders as single-account. Sites that own their auth context (e.g. oc-www's local `useAuth`) can leave this unset until they add multi-account support. | [chrome/account-menu.tsx:124](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L124) |
| &lt;a id="property-setdisplayidentity">&lt;/a> `setDisplayIdentity` | (`kind`: `DisplayIdentityKind`) => `Promise`\&lt;`void`\> | Promote a linked identity to be the badge label across every `.ochk.io` site. The connected `<OcAccountMenu>` wires this to `useOcSession().setDisplayIdentity`; a site running its own auth context passes its own equivalent. | [chrome/account-menu.tsx:115](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L115) |
| &lt;a id="property-signout">&lt;/a> `signOut` | (`opts?`: \&#123; `scope?`: `any`; \&#125;) => `void` \| `Promise`\&lt;`void`\> | Sign out. Accepts an optional `{ scope }` so the multi-account "leave this account" affordance can route through the same hook. Defaults to `'all'` for back-compat. | [chrome/account-menu.tsx:107](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L107) |
| &lt;a id="property-status">&lt;/a> `status` | `"loading"` \| `"authenticated"` \| `"anonymous"` \| `"error"` | - | [chrome/account-menu.tsx:86](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L86) |
| &lt;a id="property-switchaccount">&lt;/a> `switchAccount?` | (`didOc`: `string`) => `Promise`\&lt;`void`\> | Multi-account · flip the active session to the given did_oc. Required when `roster` is non-empty (otherwise the picker rows have nowhere to call). | [chrome/account-menu.tsx:130](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L130) |
| &lt;a id="property-tabpinned">&lt;/a> `tabPinned?` | `boolean` | Per-tab pinning · `true` when this tab holds its own session token and operates as `account` independently of the shared `.ochk.io` cookie (switching accounts in another tab won't change this one). Renders a quiet `· this tab` hint in the signed-in header. Optional — omitted by pre-pinning auth contexts. | [chrome/account-menu.tsx:146](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/account-menu.tsx#L146) |
