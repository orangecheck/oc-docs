export const metadata = {
    title: "Function: canonicalizeFunctionCall()",
    description: "Auto-generated API reference for Function: canonicalizeFunctionCall(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/agent-openai**](../README.mdx)

***

[@orangecheck/agent-openai](../README.mdx) / canonicalizeFunctionCall

# Function: canonicalizeFunctionCall()

```ts
function canonicalizeFunctionCall(c: OpenAiFunctionCall): Uint8Array;
```

Defined in: [index.ts:108](https://github.com/orangecheck/oc-packages/blob/main/agent-openai/src/index.ts#L108)

Produce the canonical bytes for an OpenAI function call. The bytes are
what the agent-action envelope's `content.hash` is a SHA-256 of.

Shape (RFC 8785 canonical JSON, then trailing LF):
  &#123;
    "arguments": &lt;canonical JSON of arguments>,
    "id":        &lt;call id>,
    "name":      &lt;function name>
  &#125;

## Parameters

| Parameter | Type |
| ------ | ------ |
| `c` | [`OpenAiFunctionCall`](../interfaces/OpenAiFunctionCall.mdx) |

## Returns

`Uint8Array`
