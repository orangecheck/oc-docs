export const metadata = {
    title: "Function: canonicalizeToolCall()",
    description: "Auto-generated API reference for Function: canonicalizeToolCall(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/agent-langgraph**](../README.mdx)

***

[@orangecheck/agent-langgraph](../README.mdx) / canonicalizeToolCall

# Function: canonicalizeToolCall()

```ts
function canonicalizeToolCall(c: LangGraphToolCall): Uint8Array;
```

Defined in: [index.ts:75](https://github.com/orangecheck/oc-packages/blob/0a6814a09006a93c94e9919f2dd5f1fbb4c3c62a/agent-langgraph/src/index.ts#L75)

Produce the canonical bytes for a LangGraph tool call.

Shape (RFC 8785 canonical JSON, then trailing LF):
  &#123;
    "args":              &lt;canonical JSON of args>,
    "call_id":           &lt;callId>,
    "graph_state_hash":  &lt;state hash>,
    "verb":              &lt;verb>
  &#125;

## Parameters

| Parameter | Type |
| ------ | ------ |
| `c` | [`LangGraphToolCall`](../interfaces/LangGraphToolCall.mdx) |

## Returns

`Uint8Array`
