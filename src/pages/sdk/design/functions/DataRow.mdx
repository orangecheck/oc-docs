export const metadata = {
    title: "Function: DataRow()",
    description: "Auto-generated API reference for Function: DataRow(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / DataRow

# Function: DataRow()

```ts
function DataRow(__namedParameters: DataRowProps): any;
```

Defined in: [composites/data-row.tsx:24](https://github.com/orangecheck/oc-packages/blob/main/design/src/composites/data-row.tsx#L24)

DataRow — the "content left, meta right, optional action" list row
reimplemented across me (federations, incidents, grants, distributions,
fee entries) and other dashboards. Pair inside a `divide-y border` list.
On narrow viewports content and meta stack; from `sm` up they sit on one
baseline-aligned line.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`DataRowProps`](../interfaces/DataRowProps) |

## Returns

`any`
