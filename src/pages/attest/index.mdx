export const metadata = {
    title: 'OC Attest',
    description:
        'Sybil resistance via proof of Bitcoin stake. The base-layer protocol in the OrangeCheck ecosystem.',
};

# OC Attest

**Proof of Bitcoin stake for the open web.** OC Attest is the base-layer
sybil-resistance primitive: a signer proves they control a Bitcoin address and
have held N sats unspent for N days. Any verifier can re-check the claim in one
HTTP call, offline, without trusting a third party.

## What it proves

A valid OC Attest attestation proves three things cryptographically:

1. **You control a specific Bitcoin address** (BIP-322 signature over a
   canonical message).
2. **The address holds `N` sats right now** (re-derived from live chain state at
   verify time).
3. **The bonded UTXO has been unspent for `N` days** (confirmation time vs.
   now).

And one thing self-assertedly:

4. **The holder claims handles** like `github:alice`, `nostr:npub1…`, etc.

Claims 1–3 are mathematical. Claim 4 is an assertion — verify handle ownership
out-of-band if your gate depends on it.

## Who this is for

- **Platforms** — forum operators, Nostr relay operators, airdrop distributors,
  DAO coordinators, Discord bot authors.
- **Integrators** — anyone who needs to gate access on a credible economic cost
  without running a KYC pipeline.
- **Signers** — anyone on the open internet who wants to stop being mistaken for
  a bot and doesn't want to hand KYC data to every platform they interact with.

## Who this is NOT for

- You need proof of personhood. Use World ID or BrightID; OC Attest is a cost
  filter, not a humanness proof.
- You need private claims. OC Attest proofs are public by design.
- You need regulatory KYC. Use a licensed provider.

## Section contents

- [**How it works**](/attest/how-it-works) — sign → publish → verify, the whole
  protocol on one page.
- [**Scoring**](/attest/scoring) — `sats_bonded`, `days_unspent`, `score_v0`
  (advisory).
- [**Verification**](/attest/verification) — the exact checks a verifier
  performs.
- [**HTTP API**](/attest/api) — `/api/check`, `/api/verify`, `/api/challenge`,
  `/api/discover`.
- [**Guide: gate an Express route**](/attest/guides/express) — drop-in
  middleware in 10 lines.
- [**Guide: filter an airdrop**](/attest/guides/airdrop) — turn a candidate list
  into a sybil-resistant allowlist.
- [**Guide: sign in with Bitcoin**](/attest/guides/sign-in-with-bitcoin) —
  signed-challenge session auth.
- [**Guide: sybil-filter a Nostr relay**](/attest/guides/nostr-relay) — drop a
  Strfry plugin in; reject events from unproofed npubs.
- [**Guide: self-host the verifier**](/attest/guides/self-hosting) — run the
  stack on your own Postgres + Vercel / Docker / VM.

## Read the shared layer first

Many of the concepts an Attest integrator needs are shared across the
OrangeCheck ecosystem:

- [Canonical message format](/ecosystem/canonical-message)
- [BIP-322 signing](/ecosystem/bip322)
- [Nostr kind-30078 publication](/ecosystem/nostr-kind-30078)
- [Conformance vectors](/ecosystem/conformance)
- [Security model](/ecosystem/security)

If you've never touched OrangeCheck before, read those first.
