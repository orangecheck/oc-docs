export const metadata = {
    title: "Function: extractAttestationIdFromUrl()",
    description: "Auto-generated API reference for Function: extractAttestationIdFromUrl(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / extractAttestationIdFromUrl

# Function: extractAttestationIdFromUrl()

```ts
function extractAttestationIdFromUrl(url: string): string | null;
```

Defined in: [attestation.ts:323](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/attestation.ts#L323)

Reverse of `getVerificationUrl` — pull an attestation id back out of a
`/attest/<id>` or legacy `/verify?id=<id>` URL. Returns `null` if the URL
shape doesn't match.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `url` | `string` |

## Returns

`string` \| `null`
