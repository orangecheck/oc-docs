export const metadata = {
    title: "Interface: OcPrimaryNavLink",
    description: "Auto-generated API reference for Interface: OcPrimaryNavLink. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / OcPrimaryNavLink

# Interface: OcPrimaryNavLink

Defined in: [chrome/primary-nav.tsx:29](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/primary-nav.tsx#L29)

`<OcPrimaryNav>` — the centered link row in every family site's
`<LayoutHeader>`. Replaces the per-site bespoke `<nav>` blocks.

**Desktop-only.** The inline row shows at `md+`; below that it is hidden,
because on mobile the same links live in the account menu's `☰` drawer
(`OcAccountMenu`, rendered to the left of the identity chip) — one shared
mobile-nav pattern, no centered hamburger, no per-site fork.

Active item is `text-primary`; siblings are muted. Active affordance is
text-only (no underline / bottom stripe) — deliberately restrained.

**Usage:**

  &lt;OcPrimaryNav
      activePath=&#123;router.pathname&#125;
      links=&#123;[
          &#123; href: '/dashboard', label: 'dashboard' &#125;,
          &#123; href: 'https://docs.ochk.io/attest', label: 'docs', external: true &#125;,
      ]&#125;
  />

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="property-external">&lt;/a> `external?` | `boolean` | When true, opens in a new tab and skips the `isActive` check. | [chrome/primary-nav.tsx:35](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/primary-nav.tsx#L35) |
| &lt;a id="property-href">&lt;/a> `href` | `string` | Same-origin path or absolute URL. | [chrome/primary-nav.tsx:31](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/primary-nav.tsx#L31) |
| &lt;a id="property-label">&lt;/a> `label` | `string` | Visible label · lowercase preferred (the chip styling applies tracking + uppercase). | [chrome/primary-nav.tsx:33](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/primary-nav.tsx#L33) |
| &lt;a id="property-matchexact">&lt;/a> `matchExact?` | `boolean` | Optional override for the active-match. By default the link is active when `activePath === href` or `activePath.startsWith(href + '/')`. | [chrome/primary-nav.tsx:38](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/primary-nav.tsx#L38) |
