export const metadata = {
    title: "Type Alias: IdentityProtocol",
    description: "Auto-generated API reference for Type Alias: IdentityProtocol. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / IdentityProtocol

# Type Alias: IdentityProtocol

```ts
type IdentityProtocol = "nostr" | "dns" | "twitter" | "github" | string;
```

Defined in: [types.ts:20](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/types.ts#L20)

Identity protocols we support first-class. Other strings remain valid per
the OCP spec ("unknown protocols MUST be preserved but MAY be ignored") —
the `| string` fallback keeps parsing open.
