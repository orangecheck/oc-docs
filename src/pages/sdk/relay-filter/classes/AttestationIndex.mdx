export const metadata = {
    title: "Class: AttestationIndex",
    description: "Auto-generated API reference for Class: AttestationIndex. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/relay-filter**](../README)

***

[@orangecheck/relay-filter](../README) / AttestationIndex

# Class: AttestationIndex

Defined in: [attestation-index.ts:93](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L93)

Every OC attestation that binds a Nostr key, held in memory and kept current
by one standing subscription. `decide()` answers from memory, so a relay's
write path never waits on the network. Bonds are re-read from the chain in
the background every `refreshMs`, which bounds how long a spent bond keeps
passing (SECURITY.md §2, chain finality).

## Constructors

### Constructor

```ts
new AttestationIndex(options?: IndexOptions): AttestationIndex;
```

Defined in: [attestation-index.ts:104](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L104)

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`IndexOptions`](../interfaces/IndexOptions) |

#### Returns

`AttestationIndex`

## Accessors

### size

#### Get Signature

```ts
get size(): number;
```

Defined in: [attestation-index.ts:205](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L205)

Attestations held, for logging.

##### Returns

`number`

## Methods

### decide()

```ts
decide(event: MinimalNostrEvent, opts?: FilterOptions): FilterDecision;
```

Defined in: [attestation-index.ts:140](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L140)

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | [`MinimalNostrEvent`](../interfaces/MinimalNostrEvent) |
| `opts` | [`FilterOptions`](../interfaces/FilterOptions) |

#### Returns

[`FilterDecision`](../interfaces/FilterDecision)

***

### ingest()

```ts
ingest(event: {
  content?: string;
  tags?: string[][];
}): boolean;
```

Defined in: [attestation-index.ts:120](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L120)

Add one attestation event. Returns true if it was new and sound.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | \&#123; `content?`: `string`; `tags?`: `string`[][]; \&#125; |
| `event.content?` | `string` |
| `event.tags?` | `string`[][] |

#### Returns

`boolean`

***

### markSynced()

```ts
markSynced(): void;
```

Defined in: [attestation-index.ts:136](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L136)

Mark the initial backlog as loaded; before this, unknown keys are "not yet", not "no".

#### Returns

`void`

***

### start()

```ts
start(): void;
```

Defined in: [attestation-index.ts:106](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L106)

#### Returns

`void`

***

### stop()

```ts
stop(): void;
```

Defined in: [attestation-index.ts:113](https://github.com/orangecheck/oc-packages/blob/main/relay-filter/src/attestation-index.ts#L113)

#### Returns

`void`
