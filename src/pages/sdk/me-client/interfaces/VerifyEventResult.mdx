export const metadata = {
    title: "Interface: VerifyEventResult",
    description: "Auto-generated API reference for Interface: VerifyEventResult. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/me-client**](../README.mdx)

***

[@orangecheck/me-client](../README.mdx) / VerifyEventResult

# Interface: VerifyEventResult

Defined in: [me-client/src/event.ts:283](https://github.com/orangecheck/oc-packages/blob/main/me-client/src/event.ts#L283)

Result of replay-verifying a billable envelope against the
 integrator's price config. Every field returned tells the caller
 WHY a divergence happened — they can render it directly.

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| &lt;a id="property-expected">&lt;/a> `expected` | \| \&#123; `gross_fee_sats`: `number`; `platform_fee_sats`: `number`; `site_rebate_sats`: `number`; `user_earned_sats`: `number`; \&#125; \| `null` | What computeFees() returned for the envelope's subtype + amount, given the integrator's current price config. Useful for the /audit divergence tracker — render expected vs actual side by side. Null when the subtype isn't enabled for this integrator. | [me-client/src/event.ts:305](https://github.com/orangecheck/oc-packages/blob/main/me-client/src/event.ts#L305) |
| &lt;a id="property-issues">&lt;/a> `issues` | \&#123; `actual`: `number` \| `null`; `expected`: `number` \| `null`; `field`: \| `"gross_fee_sats"` \| `"platform_fee_sats"` \| `"user_earned_sats"` \| `"site_rebate_sats"` \| `"subtype_disabled"` \| `"subtype_unconfigured"` \| `"percent_amount_missing"`; `message`: `string`; \&#125;[] | Issues found during verification, with field-level granularity so callers can highlight exactly which line is off. | [me-client/src/event.ts:288](https://github.com/orangecheck/oc-packages/blob/main/me-client/src/event.ts#L288) |
| &lt;a id="property-ok">&lt;/a> `ok` | `boolean` | True iff every expected fee equals the recorded fee. | [me-client/src/event.ts:285](https://github.com/orangecheck/oc-packages/blob/main/me-client/src/event.ts#L285) |
