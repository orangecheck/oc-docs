export const metadata = {
    title: "Function: AppearanceControls()",
    description: "Auto-generated API reference for Function: AppearanceControls(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / AppearanceControls

# Function: AppearanceControls()

```ts
function AppearanceControls(__namedParameters: {
  className?: string;
}): any;
```

Defined in: [tokens/appearance-menu.tsx:42](https://github.com/orangecheck/oc-packages/blob/main/design/src/tokens/appearance-menu.tsx#L42)

AppearanceControls — the mode segment + skin list, with no trigger/popover
chrome of its own. Embed it anywhere a surface wants to host appearance
settings inline (the standalone OcAppearanceMenu popover, the mobile account
menu, a site's Sheet drawer). Requires `<OcThemeProvider>` + next-themes
above it, like OcAppearanceMenu.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | \&#123; `className?`: `string`; \&#125; |
| `__namedParameters.className?` | `string` |

## Returns

`any`
