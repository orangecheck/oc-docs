export const metadata = {
    title: "Function: SectionHeader()",
    description: "Auto-generated API reference for Function: SectionHeader(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / SectionHeader

# Function: SectionHeader()

```ts
function SectionHeader(__namedParameters: SectionHeaderProps): any;
```

Defined in: [composites/section-header.tsx:32](https://github.com/orangecheck/oc-packages/blob/main/design/src/composites/section-header.tsx#L32)

SectionHeader — the canonical "§ x" section header used across every
/me and /vault dashboard surfaces. Lifted from oc-me-web's
`me/ui/SectionHeader` so the visual contract stays single-source.

Pattern is `label-mono text-primary mb-3` with a "§ " glyph followed by
the label, plus an optional muted/uppercase right-side meta string.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`SectionHeaderProps`](../interfaces/SectionHeaderProps) |

## Returns

`any`
