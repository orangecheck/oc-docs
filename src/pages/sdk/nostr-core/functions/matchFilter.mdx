export const metadata = {
    title: "Function: matchFilter()",
    description: "Auto-generated API reference for Function: matchFilter(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/nostr-core**](../README)

***

[@orangecheck/nostr-core](../README) / matchFilter

# Function: matchFilter()

```ts
function matchFilter(event: NostrEvent, filter: Filter): boolean;
```

Defined in: [index.ts:273](https://github.com/orangecheck/oc-packages/blob/main/nostr-core/src/index.ts#L273)

True when `event` satisfies every constraint in `filter` (NIP-01 semantics; `limit` is ignored).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | [`NostrEvent`](../interfaces/NostrEvent) |
| `filter` | [`Filter`](../interfaces/Filter) |

## Returns

`boolean`
