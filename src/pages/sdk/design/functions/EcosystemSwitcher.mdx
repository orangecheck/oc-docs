export const metadata = {
    title: "Function: EcosystemSwitcher()",
    description: "Auto-generated API reference for Function: EcosystemSwitcher(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/design**](../README)

***

[@orangecheck/design](../README) / EcosystemSwitcher

# Function: EcosystemSwitcher()

```ts
function EcosystemSwitcher(__namedParameters: EcosystemSwitcherProps): any;
```

Defined in: [chrome/ecosystem-switcher.tsx:190](https://github.com/orangecheck/oc-packages/blob/main/design/src/chrome/ecosystem-switcher.tsx#L190)

Family chrome.

Auth- and family-coupled composites: the shared header logo/dropdown,
account menu, primary nav, ecosystem switcher, dashboard shells, and the
family-properties registry. These depend on `@orangecheck/auth-client` and
the family map, so they live in their own tier separate from the
dependency-free primitives and the generic composites.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`EcosystemSwitcherProps`](../interfaces/EcosystemSwitcherProps) |

## Returns

`any`
