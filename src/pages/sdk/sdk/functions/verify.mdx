export const metadata = {
    title: "Function: verify()",
    description: "Auto-generated API reference for Function: verify(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/sdk**](../README.mdx)

***

[@orangecheck/sdk](../README.mdx) / verify

# Function: verify()

```ts
function verify(input: VerifyInput, opts?: VerifyOptions): Promise<VerifyOutcome>;
```

Defined in: [verify.ts:491](https://github.com/orangecheck/oc-packages/blob/main/sdk/src/verify.ts#L491)

Verify an OrangeCheck attestation

This is the main verification function that:
1. Validates the canonical message format
2. Verifies the Bitcoin signature (BIP-322 or legacy)
3. Fetches and analyzes UTXOs
4. Computes reputation metrics

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `input` | [`VerifyInput`](../interfaces/VerifyInput.mdx) | Verification input (message, address, signature) |
| `opts` | [`VerifyOptions`](../interfaces/VerifyOptions.mdx) | Optional verification options (demo mode, test mode, etc.) |

## Returns

`Promise`\&lt;[`VerifyOutcome`](../interfaces/VerifyOutcome.mdx)\>

Verification outcome with status codes and metrics

## Example

```typescript
const result = await verify({
  msg: canonicalMessage,
  addr: 'bc1q...',
  sig: 'AkcwRAIg...',
});

if (result.ok) {
  console.log('Score:', result.metrics?.score);
}
```
