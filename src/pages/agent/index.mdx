export const metadata = {
    title: 'OC Agent',
    description:
        'Bitcoin-identity-bound delegation authority. A principal grants an autonomous agent scoped action rights, bonded to sats, revocable on-demand. Every action the agent takes is non-repudiable and offline-verifiable.',
};

# OC Agent

**Every agent gets a Bitcoin address. Every action, signed.** OC Agent is the
delegation-authority primitive in the OrangeCheck family: a principal grants an
autonomous agent — a script, a bot, a pipeline, an LLM — scoped action rights
bound to the principal's Bitcoin identity. Every action the agent takes is
non-repudiable, offline-verifiable, and contained by scope + time. No OAuth, no
custodial tokens, no trusted issuer.

## What it proves

A valid Agent action envelope proves three things cryptographically:

1. **The action was issued under a valid delegation.** The delegation is
   BIP-322-signed by the principal's Bitcoin address.
2. **The action is within the granted scope.** Scopes are declarative strings
   like `lock:seal(recipient=bc1qalice)` or
   `ln:send(max_sats<=1000,node=03abc…)`; the verifier matches the action
   against the granted scope set deterministically.
3. **The delegation is not revoked and not expired.** Revocation is advertised
   via [Nostr kind-30085](/ecosystem/nostr-kind-30078); expiry is a plain
   timestamp in the delegation record.

And optionally, with an Attest reference:

4. **The principal held `N` sats for `N` days at the moment of delegation.** A
   stake-at-signing signal that verifiers can re-resolve from live chain state.

## Who this is for

- **CI / release pipelines** that sign artifacts, stamps, or envelopes on a
  principal's behalf without handing the pipeline the Bitcoin key.
- **Autonomous trading / LN routing bots** that need a hard ceiling on blast
  radius — `max_sats<=N`, `recipient in {…}`, `max_actions_per_minute<=M`.
- **Social / Nostr bots** that post under a human's identity on a revocable,
  scoped leash.
- **LLM agents** signing calls to downstream tools where the operator wants a
  tamper-evident audit trail of "what the agent did, under whose authority."

## Who this is NOT for

- **Transferring the principal's Bitcoin key.** The principal's key never leaves
  their wallet. The agent holds only the delegation record and its own keypair.
- **Transitive re-delegation.** An agent cannot issue sub-delegations in v0. If
  you need an agent to delegate further, re-mint a delegation from the principal
  that targets the sub-agent.
- **Replacing centralized IAM.** OAuth + role-based access is fine when there's
  a central operator. OC Agent is the answer when the identity root is a Bitcoin
  address, not an issuer.

## Section contents

- [**Why OC Agent**](/agent/why) — what breaks without it, why Bitcoin-bound
  delegation, what the alternatives can't do.
- [**Quickstart**](/agent/quickstart) — mint a delegation, sign an action,
  verify it downstream. Five minutes.
- [**Protocol walkthrough**](/agent/protocol) — the three envelopes (delegation,
  action, revocation), the two canonical flows, diagrams.
- [**Scope grammar**](/agent/scopes) — BNF, sub-scope containment, constraint
  semantics, the 8 MVP scopes.
- [**Security posture**](/agent/security) — revocation timing, blast radius,
  what scopes cannot constrain.
- [**Specification**](/agent/spec) — normative rules; the reference impls in
  `oc-packages` are required to pass its conformance vectors.

## Packages

- `@orangecheck/agent-core` — canonical delegation + action envelope format,
  scope parser, verifier.
- `@orangecheck/agent-signer` — helpers for signing delegations (principal) and
  actions (agent).
- `@orangecheck/agent-cli` — `oc-agent` shell for minting / revoking / verifying
  envelopes.
- `@orangecheck/agent-mcp` — Model Context Protocol bridge so LLM toolchains can
  produce agent-signed tool calls directly.

See [SDK overview](/sdks).

## Shared ecosystem concepts

Agent inherits the same plumbing every OrangeCheck protocol uses:

- [Canonical message format](/ecosystem/canonical-message) — the delegation
  record is a canonical message.
- [BIP-322 signing](/ecosystem/bip322) — the principal's delegation signature.
- [Nostr publication](/ecosystem/nostr-kind-30078) — published delegations live
  on kind-30083; revocations on kind-30085; agent-action envelopes share the OC
  Stamp transport on kind-30084.
- [Conformance vectors](/ecosystem/conformance) — the cross-impl test suite.
- [Security model](/ecosystem/security) — shared threat framework.

## Spec + implementation

- Normative spec:
  [`oc-agent-protocol/SPEC.md`](https://github.com/orangecheck/oc-agent-protocol)
- Reference impls: `@orangecheck/agent-core`, `agent-signer`, `agent-cli`,
  `agent-mcp` in [`oc-packages`](https://github.com/orangecheck/oc-packages)
- Reference site: `agent.ochk.io`
