export const metadata = {
    title: "Function: verifyPoll()",
    description: "Auto-generated API reference for Function: verifyPoll(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vote-core**](../README)

***

[@orangecheck/vote-core](../README) / verifyPoll

# Function: verifyPoll()

```ts
function verifyPoll(poll: Poll, verify: SignatureVerifier): Promise<VerifyResult>;
```

Defined in: [vote-core/src/verify.ts:46](https://github.com/orangecheck/oc-packages/blob/main/vote-core/src/verify.ts#L46)

Structural checks from SPEC §3.3 plus the creator's BIP-322 signature over
`poll_id` (§10.1). A poll that fails MUST be rejected.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `poll` | [`Poll`](../interfaces/Poll) |
| `verify` | [`SignatureVerifier`](../type-aliases/SignatureVerifier) |

## Returns

`Promise`\&lt;[`VerifyResult`](../type-aliases/VerifyResult)\>
