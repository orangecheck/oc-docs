export const metadata = {
    title: "Function: OcSignInButton()",
    description: "Auto-generated API reference for Function: OcSignInButton(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/auth-client**](../README.mdx)

***

[@orangecheck/auth-client](../README.mdx) / OcSignInButton

# Function: OcSignInButton()

```ts
function OcSignInButton(__namedParameters: OcSignInButtonProps): any;
```

Defined in: [components.tsx:349](https://github.com/orangecheck/oc-packages/blob/main/auth-client/src/components.tsx#L349)

Drop-in sign-in button. Renders an anchor that points at either:

  - the consumer's local `/signin` page (when `signInUrl` prop is set
    — recommended; works with `<OcSignIn />`), or
  - the auth host's `/signin?return_to=…` (default fallback)

When the user is already authenticated it renders nothing.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`OcSignInButtonProps`](../interfaces/OcSignInButtonProps.mdx) |

## Returns

`any`
