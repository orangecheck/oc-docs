export const metadata = {
    title: "Function: resolveSecretRef()",
    description: "Auto-generated API reference for Function: resolveSecretRef(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/vault-core**](../README.mdx)

***

[@orangecheck/vault-core](../README.mdx) / resolveSecretRef

# Function: resolveSecretRef()

```ts
function resolveSecretRef(entries: DecryptedEntry[], ref: string | SecretRef): string;
```

Defined in: [vault-core/src/refs.ts:105](https://github.com/orangecheck/oc-packages/blob/main/vault-core/src/refs.ts#L105)

Resolve a reference against a set of already-decrypted entries. `entries`
must be the entries of the reference's `vault` — scoping the vault is the
caller's responsibility (see `OcVault.resolve`).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `entries` | [`DecryptedEntry`](../interfaces/DecryptedEntry.mdx)[] |
| `ref` | `string` \| [`SecretRef`](../interfaces/SecretRef.mdx) |

## Returns

`string`
