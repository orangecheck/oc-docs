export const metadata = {
    title: "Type Alias: SignFn",
    description: "Auto-generated API reference for Type Alias: SignFn. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/wallet-adapter**](../README.mdx)

***

[@orangecheck/wallet-adapter](../README.mdx) / SignFn

# Type Alias: SignFn

```ts
type SignFn = (message: string) => Promise<string>;
```

Defined in: [types.ts:8](https://github.com/orangecheck/oc-packages/blob/0a6814a09006a93c94e9919f2dd5f1fbb4c3c62a/wallet-adapter/src/types.ts#L8)

Every supported wallet produces a signature the same way from our API's
perspective: we hand it a canonical message string, we get back a BIP-322
signature as a base64 or hex string.

Individual wallet shims translate their quirky APIs into this shape.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

## Returns

`Promise`\&lt;`string`\>
