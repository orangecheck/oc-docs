export const metadata = {
    title: "Variable: OcBadge",
    description: "Auto-generated API reference for Variable: OcBadge. Source: TypeScript types in oc-packages.",
};

[**@orangecheck/react**](../README.mdx)

***

[@orangecheck/react](../README.mdx) / OcBadge

# Variable: OcBadge

```ts
const OcBadge: MemoExoticComponent<(__namedParameters: OcBadgeProps) => Element>;
```

Defined in: [src/badge.tsx:199](https://github.com/orangecheck/oc-packages/blob/0a6814a09006a93c94e9919f2dd5f1fbb4c3c62a/react/src/badge.tsx#L199)

@orangecheck/react

Three focused components. One pill, one gate, one signing button.

  &lt;OcBadge>             — display a proof-of-Bitcoin-stake badge
  &lt;OcGate>              — client-side conditional render by OC status
  &lt;OcChallengeButton>   — run the signed-challenge auth flow in-browser

All three are framework-agnostic React components — no styling framework
required, no global CSS, no state store. Inline styles are used so the
components render correctly in any host app.
