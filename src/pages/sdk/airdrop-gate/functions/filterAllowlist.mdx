export const metadata = {
    title: "Function: filterAllowlist()",
    description: "Auto-generated API reference for Function: filterAllowlist(). Source: TypeScript types in oc-packages.",
};

[**@orangecheck/airdrop-gate**](../README.mdx)

***

[@orangecheck/airdrop-gate](../README.mdx) / filterAllowlist

# Function: filterAllowlist()

```ts
function filterAllowlist(addresses: string[], options?: FilterAllowlistOptions): Promise<FilterAllowlistResult>;
```

Defined in: [index.ts:100](https://github.com/orangecheck/oc-packages/blob/0a6814a09006a93c94e9919f2dd5f1fbb4c3c62a/airdrop-gate/src/index.ts#L100)

Filter a candidate address list against OrangeCheck thresholds.

The `addresses` array is deduplicated before checking — duplicates would
just waste API calls and never change the outcome.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `addresses` | `string`[] |
| `options` | [`FilterAllowlistOptions`](../interfaces/FilterAllowlistOptions.mdx) |

## Returns

`Promise`\&lt;[`FilterAllowlistResult`](../interfaces/FilterAllowlistResult.mdx)\>
